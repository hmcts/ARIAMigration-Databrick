steps:
  - task: UsePythonVersion@0
    displayName: 'Use Python 3.x'
    inputs:
      versionSpec: '3.x'

  - script: |
      pip install uv
      uv pip install -r $(Build.SourcesDirectory)/tests/requirements.txt --system
    displayName: 'Install dependencies'
  
  - script: |
      echo "Running decided_a unit tests"
      pytest $(Build.SourcesDirectory)/tests/active/decided_a \
        --junitxml=$(Build.ArtifactStagingDirectory)/pytest-results.xml -v
    displayName: 'Run decided_a state pytest unit tests'
  