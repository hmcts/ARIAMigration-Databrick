bundle:
  name: ARIA_AssetBundle_Deployment

include:
  - "resources/pipelines/*.yml"
  - "resources/workflows/*.yml"

workspace: &sandbox
  host: $(DATABRICKS_HOST)
  
targets:
  sandbox:
      workspace: *sandbox
      mode : development
      default : true
