resources:
  pipelines:
    hive_pipeline_pl_reasonsForAppealSubmitted_gold:
      name: HIVE_PL_reasonsForAppealSubmitted_gold
      configuration:
        pipelines.tableManagedByMultiplePipelinesCheck.enabled: "false"
      clusters:
        - label: default
          instance_pool_id: ${var.instance_pool_id}
          autoscale:
            min_workers: 1
            max_workers: 8
            mode: ENHANCED
      libraries:
        - notebook:
            path: "/Workspace/live/ACTIVE_FROM_MASTER/APPEALS/GOLD_REASON_FOR_APPEAL_SUBMITTED_JSON"
      target: hive_reasonsForAppealSubmitted_gold
      development: true