resources:
  pipelines:
    pipeline_pl_ariadm_active_appeals_bronze:
      name: PL_ARIADM_ACTIVE_APPEALS_BRONZE
      clusters:
        - label: default
          instance_pool_id: ${var.instance_pool_id}
          autoscale:
            min_workers: 5
            max_workers: 8
            mode: ENHANCED
      libraries:
        - notebook:
            path: "/Workspace/live/ACTIVE/APPEALS/BRONZE_ACTIVE_APPEALS"
      target: ariadm_active_appeals
      development: true
