resources:
  jobs:
    db_wf_bail_recieve:
      name: db_wf_bail_recieve
      tasks:
        - task_key: collect_responses
          notebook_task:
            notebook_path: /Repos/ara.islam1@hmcts.net/ARIAMigration-Databrick/Databricks/ARCHIVE/BAILS/bails_resp_autoloader
            source: WORKSPACE
          existing_cluster_id: 0930-233618-mrfsuf2j
        - task_key: bails_receive_dashboard
          depends_on:
            - task_key: collect_responses
          notebook_task:
            notebook_path: /Workspace/Users/andrew.mcdevitt@hmcts.net/Bails Receive Audit
              Dashboard
            source: WORKSPACE
          job_cluster_key: Job_cluster
      job_clusters:
        - job_cluster_key: Job_cluster
          new_cluster:
            spark_version: 15.4.x-scala2.12
            azure_attributes:
              first_on_demand: 1
              availability: SPOT_WITH_FALLBACK_AZURE
              spot_bid_max_price: -1
            node_type_id: Standard_D4ds_v5
            spark_env_vars:
              PYSPARK_PYTHON: /databricks/python3/bin/python3
            enable_elastic_disk: true
            data_security_mode: LEGACY_SINGLE_USER_STANDARD
            runtime_engine: PHOTON
            num_workers: 8
      queue:
        enabled: true
