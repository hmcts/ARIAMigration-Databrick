bundle:
  name: ARIA_DAB_INGEST02
#  git:
#    origin_url: https://github.com/hmcts/ARIAMigration-Databrick
#    branch: main

workspace:
  host: https://adb-1879076228317698.18.azuredatabricks.net

permissions:
  - level: CAN_MANAGE
    service_principal_name: 93076eb6-260a-486b-be27-b79035a64fb1 

# To update .whl in future once DABs are working
#artifacts:
#  <some-unique-artifact-identifier>:
#    build: string
#    files:
#      - source: string
#    path: string
#    type: string   

run_as:
  service_principal_name: ServicePrincipalCICD

resources:
  jobs:
    db_wf_apl_fta_send:
      path: ${bundle.file(resources/workflows/db_wf_apl_fta_send.yml)}
    db_wf_bail_receive: 
      path: ${bundle.file(resources/workflows/db_wf_bail_receive.yml)}
    db_wf_bail_reprocessing: 
      path: ${bundle.file(resources/workflows/db_wf_bail_reprocessing.yml)}
    db_wf_bail_send:
      path: ${bundle.file(resources/workflows/db_wf_bail_send.yml)}
    db_wf_joh_receive: 
      path: ${bundle.file(resources/workflows/db_wf_joh_receive.yml)}
    db_wf_joh_send: 
      path: ${bundle.file(resources/workflows/db_wf_joh_send.yml)}
    db_wf_td_receive:
      path: ${bundle.file(resources/workflows/db_wf_td_receive.yml)}
    db_wf_td_send: 
      path: ${bundle.file(resources/workflows/db_wf_td_send.yml)}
  
  pipelines:
    pipeline_ARIA_ARCHIVE_BAILS: 
      path: ${bundle.file(resources/pipelines/ARIA_ARCHIVE_BAILS.yml)}
    pipeline_PL_ARIADM_ARM_APPEALS: 
      path: ${bundle.file(resources/pipelines/PL_ARIADM_ARM_APPEALS.yml)}
    pipeline_PL_ARIADM_ARM_FTA: 
      path: ${bundle.file(resources/pipelines/PL_ARIADM_ARM_FTA.yml)}
    pipeline_PL_ARIADM_ARM_JOH: 
      path: ${bundle.file(resources/pipelines/PL_ARIADM_ARM_JOH.yml)}
    pipeline_PL_ARIADM_ARM_TD: 
      path: ${bundle.file(resources/pipelines/PL_ARIADM_ARM_TD.yml)}

targets:
  sandbox:
      workspace: $(workspace)