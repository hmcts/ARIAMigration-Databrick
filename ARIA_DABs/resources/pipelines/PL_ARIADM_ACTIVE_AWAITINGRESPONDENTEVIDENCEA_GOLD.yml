resources:
  pipelines:
    pipeline_pl_awaitingRespondentEvidenceA_gold:
      name: PL_awaitingRespondentEvidenceA_gold
      configuration:
        pipelines.tableManagedByMultiplePipelinesCheck.enabled: "false"
      clusters:
        - label: default
          instance_pool_id: ${var.instance_pool_id}
          autoscale:
            min_workers: 1
            max_workers: 8
            mode: ENHANCED
      libraries:
        - notebook:
            path: "/Workspace/live/ACTIVE/APPEALS/GOLD_AWAITING_EVIDENCE_RESPONDENT_A_JSON"
      target: awaitingRespondentEvidenceA_gold
      development: true