resources:
  pipelines:
    pipeline_pl_caseUnderReview_gold:
      name: PL_caseUnderReview_gold
      configuration:
        pipelines.tableManagedByMultiplePipelinesCheck.enabled: "false"
      clusters:
        - label: default
          instance_pool_id: ${var.instance_pool_id}
          autoscale:
            min_workers: 1
            max_workers: 8
            mode: ENHANCED
      libraries:
        - notebook:
            path: "/Workspace/live/ACTIVE/APPEALS/GOLD_CASE_UNDER_REVIEW_JSON"
      target: caseUnderReview_gold
      development: true