resources:
  pipelines:
    pipeline_aria_archive_bails_uc:
      name: ARIA_ARCHIVE_BAILS_UC

      catalog: aria_sbox00
      target: active

      development: true

      clusters:
        - label: default
          instance_pool_id: ${var.instance_pool_id}
          autoscale:
            min_workers: 5
            max_workers: 8
            mode: ENHANCED

      libraries:
        - notebook:
            path: "/Workspace/live/ARCHIVE/BAIL/Dynamic-ARIA-Bails v2"